apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingestion-service
  namespace: talvra
  labels:
    app: ingestion-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ingestion-service
  template:
    metadata:
      labels:
        app: ingestion-service
    spec:
      imagePullSecrets:
        - name: acr-pull
      containers:
        - name: ingestion-service
          image: oldwhisper.azurecr.io/hwrk-ingestion-service:1.2
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4010
              name: http
          envFrom:
            - secretRef:
                name: talvra-secrets
          readinessProbe:
            httpGet:
              path: /ingestion/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /ingestion/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: ingestion-service
  namespace: talvra
  labels:
    app: ingestion-service
spec:
  type: ClusterIP
  ports:
    - port: 4010
      targetPort: http
      name: http
  selector:
    app: ingestion-service

