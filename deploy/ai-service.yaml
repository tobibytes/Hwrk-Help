apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-service
  namespace: talvra
  labels:
    app: ai-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-service
  template:
    metadata:
      labels:
        app: ai-service
    spec:
      imagePullSecrets:
        - name: acr-pull
      containers:
        - name: ai-service
          image: oldwhisper.azurecr.io/hwrk-ai-service:1.2
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4020
              name: http
          envFrom:
            - secretRef:
                name: talvra-secrets
          readinessProbe:
            httpGet:
              path: /ai/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /ai/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: talvra
  labels:
    app: ai-service
spec:
  type: ClusterIP
  ports:
    - port: 4020
      targetPort: http
      name: http
  selector:
    app: ai-service

