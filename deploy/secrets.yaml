# Placeholder Kubernetes Secret. DO NOT commit real secrets to git.
# Copy this to deploy/secrets.local.yaml and fill locally; keep this placeholder in the repo.
apiVersion: v1
kind: Secret
metadata:
  name: talvra-secrets
  namespace: talvra
type: Opaque
stringData: {}
