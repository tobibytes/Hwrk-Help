apiVersion: apps/v1
kind: Deployment
metadata:
  name: canvas-service
  namespace: talvra
  labels:
    app: canvas-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: canvas-service
  template:
    metadata:
      labels:
        app: canvas-service
    spec:
      imagePullSecrets:
        - name: acr-pull
      containers:
        - name: canvas-service
          image: oldwhisper.azurecr.io/hwrk-canvas-service:1.2
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4002
              name: http
          envFrom:
            - secretRef:
                name: talvra-secrets
          readinessProbe:
            httpGet:
              path: /canvas/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /canvas/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: canvas-service
  namespace: talvra
  labels:
    app: canvas-service
spec:
  type: ClusterIP
  ports:
    - port: 4002
      targetPort: http
      name: http
  selector:
    app: canvas-service

